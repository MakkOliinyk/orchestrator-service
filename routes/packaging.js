const packagingRoutes = async (fastify) => {
    fastify.post('/packaging/pack', async (request, reply) => {

    });

    fastify.post('/packaging/unpack', async (request, reply) => {

    });
};

export default packagingRoutes;
